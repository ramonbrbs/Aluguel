<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_menu"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="br.ufba.dcc.aluguel.MenuActivity">

    <Button
        android:text="Cadastrar Quarto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btnPesquisar"
        android:layout_alignRight="@+id/btnPesquisar"
        android:layout_alignEnd="@+id/btnPesquisar"
        android:layout_marginTop="24dp"
        android:id="@+id/btnCadastrar" />

    <Button
        android:text="Sair"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btnCadastrar"
        android:layout_alignRight="@+id/btnCadastrar"
        android:layout_alignEnd="@+id/btnCadastrar"
        android:layout_marginTop="33dp"
        android:id="@+id/btnSair" />

    <Button
        android:text="Pesquisar Quarto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="101dp"
        android:id="@+id/btnPesquisar"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />
</RelativeLayout>
